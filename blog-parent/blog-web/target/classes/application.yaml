server:
  port: 8080

spring:
  application:
    name: blog-system
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/personal_blog?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  # 2. Jackson?? (??JSON????)
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  # MyBatis Plus ??
  mybatis-plus:
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # ??SQL???????
      map-underscore-to-camel-case: true # ????????
    global-config:
      db-config:
        id-type: auto # ????
# 4. ??????? (????JWT?????????????)
jwt:
  secret: doom-blog-system-secure-key-2025-project-best-practice
  expiration: 86400000 # 24??
  header: Authorization

# 5. ????
logging:
  level:
    com.doom: debug # ??????????
    org.springframework.security: info # ??Security?????debug????